class Winter {
  constructor(image) {
    this.image = image;
  }
  
  display() {
          background(80);
    image(this.image, 0, 0, width, height);
        image(winterSky, 0, 0, 800, 300);
  
      
    image(winterImage,0,0,width,height)
  
   image(winterFrontImage, 0, 0, 800, 300);
  }
}
